version: '3.8'

services:
  dataapi:
    build: .
    container_name: dataapi
    ports:
      - "80:80"
    environment:
      - NAME=World
    volumes:
      - .:/app
    networks:
      - app-network